<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/padding_default" >

    <EditText
        android:id="@+id/i_forget_password_phone"
        style="@style/single_view_et"
        android:hint="输入手机号"
        android:inputType="phone" />

    <LinearLayout style="@style/single_view" >

        <EditText
            android:id="@+id/i_forget_password_vcode"
            style="@style/form_left_et"
            android:hint="6位数字的验证码"
            android:textColor="@color/black" />

        <Button
            android:id="@+id/i_forget_password_getvcode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:text="获取验证码"
            android:textColor="@color/black" />
    </LinearLayout>

    <EditText
        android:id="@+id/i_forget_password_newpassword"
        style="@style/single_view_et"
        android:hint="输入新密码(8-16位非特殊字符)"
        android:inputType="textPassword" />

    <Button
        android:id="@+id/i_forget_password_submit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="确定"
        android:textColor="@color/black" />

</LinearLayout>